# 🛡️ سیستم کنترل دسترسی هوشمند SAS

🔗 **مخزن گیت‌هاب پروژه:** https://github.com/mohammadnjt/sas

سیستم SAS یک دستگاه کنترل تردد هوشمند مبتنی بر ماژول ESP32-CAM و NFC است که به عنوان یک کلاینت فرمان‌پذیر (Dumb Terminal) عمل کرده و برای پردازش لاجیک، با سرور اختصاصی شما ارتباط برقرار می‌کند.

---

## 🚀 راهنمای راه‌اندازی و اتصال (Setup Guide)

1. **روشن کردن دستگاه:** پس از اتصال دستگاه SAS به منبع تغذیه، دستگاه روشن شده و به‌طور پیوسته روشن می‌ماند.
2. **حالت هات‌اسپات (AP Mode):** در صورتی که دستگاه برای اولین بار روشن شده باشد یا نتواند به شبکه وای‌فای قبلی متصل شود، به صورت خودکار یک شبکه هات‌اسپات (Hotspot) ایجاد می‌کند:
   * **نام شبکه (SSID):** `SAS-Setup`
   * **رمز عبور:** `12345678`
3. **ورود به پنل تنظیمات:** پس از اتصال با گوشی یا لپ‌تاپ به این شبکه، مرورگر خود را باز کرده و دقیقاً آدرس زیر را به همراه پورت وارد کنید:
   * 🌐 `http://192.168.4.1:9000`
4. **پیکربندی شبکه و سرور:** وارد منوی `System Config` شوید. در این بخش می‌توانید اطلاعات شبکه وای‌فای (SSID و Password) و همچنین **آدرس سرور (Server URL)** اختصاصی خود را وارد و ذخیره کنید تا دستگاه به سیستم بک‌اند شما متصل شود.

---

## 📚 مستندات API سرور پشتیبان (Backend Integration)

### 🔐 ۱. امنیت و احراز هویت (Authentication)

برای جلوگیری از نفوذ و جعل درخواست‌ها، تمامی ارتباطات میان سخت‌افزار و سرور مجهز به سیستم رمزنگاری است.

* دستگاه در هر درخواست، هدر امنیتی `X-Auth-Token` را ارسال می‌کند.
* **فرمول ساخت توکن:**

  ```
  X-Auth-Token = SHA256(ADMIN_API_KEY + device_virtual_id + unix_timestamp)
  ```

* *نکته توسعه‌دهنده:* سرور موظف است در هر درخواست، این هدر را خوانده و تطبیق دهد (در نظر داشته باشید که ارسال متغیر `unix_timestamp` برای جلوگیری از حملات Replay در آپدیت‌های بعدی به سورس سخت‌افزار اضافه می‌شود، اما منطق آن را از هم‌اکنون در بک‌اند خود لحاظ کنید).

### 📡 ۲. مسیر ارتباطی یکپارچه (Single Endpoint)

با معماری جدید، تمامی درخواست‌های دستگاه به روت سرور شما با استفاده از پارامتر `op` ارسال می‌شوند.

#### 🔹 حالت آنلاین (اسکن بی‌درنگ)

هنگام قرار گرفتن کارت روی دستگاه، فوراً درخواست زیر ارسال می‌شود:

```http
GET {SERVER_URL}/?op=nfc&uid={UID}
```

`uid`: آدرس MAC کارت اسکن‌شده (مثال: `59:EB:65:79`)

#### ⏳ حالت آفلاین (همگام‌سازی با تأخیر)

اگر دستگاه اتصال شبکه را از دست بدهد، لاگ‌ها ذخیره شده و پس از اتصال مجدد به شبکه، هر ۱۰ دقیقه تلاش می‌کند تا آن‌ها را به سرور بفرستد:

```http
GET {SERVER_URL}/?op=nfc&uid={UID}&log_date={DATE}&log_time={TIME}&is_offline=1
```

⚠️ **نکته مهم:** وقتی سرور شما پارامتر `is_offline=1` را دریافت می‌کند، فقط باید داده‌ها را در دیتابیس ثبت کند و نیازی به اعتبارسنجی بی‌درنگ نیست (چون شخص قبلاً محل را ترک کرده است).

### 📥 ۳. پاسخ موردانتظار سرور

سرور باید با وضعیت HTTP 200 OK و یک محمولهٔ JSON پاسخ دهد. این محموله نحوهٔ واکنش سخت‌افزار را مشخص می‌کند:

```json
{
  "status": "OK",
  "uid": "47:35:35:02",
  "message": "Welcome Boss!",
  "action": "1101",
  "date": "1405/03/28",
  "time": "2026-06-18T15:09:41Z"
}
```

### ⚙️ ۴. کدهای عملکردی سخت‌افزار (Action Codes)

کلید `action` در پاسخ JSON یک کد باینری ۴ بیتی است که به‌ترتیب وضعیت **[رله] [بیزر] [LED قرمز] [LED سبز]** را کنترل می‌کند:

| کد Action | وضعیت | رفتار سخت‌افزاری |
|---|---|---|
| `1101` | مجاز (Granted) | رله فعال (در باز می‌شود) + بوق تایید + LED سبز |
| `0110` | غیرمجاز (Denied) | رله بسته + بوق هشدار + LED قرمز |
| `0100` | تکراری (Anti-Passback) | فقط یک بوق کوتاه هشدار |
| `0000` | حالت سایه (Ghost) | هیچ واکنشی نشان نمی‌دهد |

> در صورت بروز خطای اتصال یا قطعی سرور (تایم‌اوت / ارور 500)، دستگاه به‌طور پیش‌فرض برای حفظ امنیت حالت `0110` را اجرا خواهد کرد.

---

## 🔄 نسخهٔ جایگزین API (برند BLH)

در کنار مسیر یکپارچهٔ بالا، یک نسخهٔ مستند دیگر از این سیستم (با برندینگ Bridge Logic Hardware) با ساختار درخواست و پاسخ کمی متفاوت نیز وجود دارد. تفاوت‌های کلیدی آن به شرح زیر است:

### درخواست آنلاین

```http
GET {SERVER_URL}/nfc.php?uid={UID}&device={DEVICE_ID}
```

| پارامتر | نوع | توضیحات |
|---|---|---|
| `uid` | رشته | آدرس MAC کارت اسکن‌شده |
| `device` | رشته | شناسه منحصربه‌فرد دستگاه (مثال: `SAS`) |

### درخواست آفلاین

```http
GET {SERVER_URL}/nfc.php?uid={UID}&device={DEVICE_ID}&log_date={DATE}&log_time={TIME}&is_offline=1
```

| پارامتر | نوع | توضیحات |
|---|---|---|
| `log_date` | رشته | تاریخ اسکن (مثال: `1405/03/30`) |
| `log_time` | رشته | زمان اسکن (مثال: `14:35`) |
| `is_offline` | عدد صحیح | همیشه `1`؛ نشان‌دهندهٔ رویداد با تأخیر است |

> وقتی `is_offline=1` دریافت می‌شود، سرور فقط باید داده را ثبت کند و نیازی به اعتبارسنجی بی‌درنگ نیست.

### پاسخ سرور

در این نسخه، پاسخ به‌جای فیلد `action` باینری، از فیلدهای زیر استفاده می‌کند:

```json
{
  "access": "granted",
  "action_state": 3,
  "message": "Welcome Boss"
}
```

| کلید | مقادیر معتبر | توضیح |
|---|---|---|
| `access` | `granted` \| `denied` | شاخهٔ منطقی اصلی پاسخ را مشخص می‌کند |
| `action_state` | عدد صحیح | ترکیبی آزاد و قابل‌تنظیم از خروجی‌هاست |
| `message` | رشته (انگلیسی) | متنی که روی LCD نمایش داده می‌شود |

> **نکته دربارهٔ `action_state`:** این مقدار یک کد ثابت و از‌پیش‌تعریف‌شده نیست. هر کدام از خروجی‌های سخت‌افزاری (مثلاً رله، LED، بازخورد صوتی) به‌صورت مستقل قابل فعال/غیرفعال شدن هستند و سرور می‌تواند هر ترکیبی از آن‌ها را انتخاب کند؛ یعنی مثلاً می‌توان همزمان `access: "denied"` فرستاد ولی `action_state` را طوری تنظیم کرد که همهٔ خروجی‌ها (مثل حالتی که در `granted` هم استفاده می‌شود) فعال شوند. به بیان دیگر، `access` و `action_state` مستقل از هم هستند و منطق نهایی واکنش سخت‌افزار کاملاً به تنظیمات سرور بستگی دارد، نه یک جدول ثابت.

> **نکته دربارهٔ `message`:** هر متنی که در این فیلد ارسال شود روی LCD چاپ می‌شود، اما دو محدودیت دارد: باید حتماً به زبان انگلیسی باشد و طول آن نباید از ۱۶ کاراکتر بیشتر شود.

> اگر پاسخ سرور ناقص، دیرهنگام یا همراه با خطای سرور باشد، به‌طور پیش‌فرض حالت `denied` اجرا می‌شود تا امنیت حفظ شود.